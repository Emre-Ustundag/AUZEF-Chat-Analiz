services:
  web:
    build:
      context: .
      dockerfile: infra/docker/web.Dockerfile
    container_name: auzef-web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
